name: smart-intersection-safety
description: deterministic traffic intersection environment

entrypoint: app:app

endpoints:
  reset: /reset
  step: /step
  state: /state

tasks:
  - name: easy-flow
  - name: medium-congestion
  - name: hard-peak-hour